"use client "
function getRandomInt(count:number){
    return Math.floor(Math.random()*count);
}
const page = () => {
    
  return (
    <div>
      Intercepted Error page
    </div>
  )
}

export default page

